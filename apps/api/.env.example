# API (FastAPI): copy to .env and fill values. See README Quick start.

# Required for LLM scope, planner, RAG compose, Serp summarization
OPENAI_API_KEY=sk-...

# Recommended: PartSelect model/part discovery, find-model links, parts fallbacks (https://serpapi.com)
SERPAPI_API_KEY=

# Optional: RAG and part lookup (Postgres + pgvector). Omit to rely on Serp/fallbacks only.
# DATABASE_URL=postgresql://user:pass@localhost:5432/partselect

# Optional: CORS origins for web app (default: http://localhost:3000)
# CORS_ORIGINS=http://localhost:3000

# Optional: use LLM-based router/planner instead of rule triage (default 1 when OPENAI_API_KEY set)
# USE_LLM_ROUTER_PLANNER=1
# USE_LLM_TRIAGE=0
# USE_LLM_SCOPE=1

# Optional: model overrides (default gpt-4o-mini)
# PLANNER_LLM_MODEL=gpt-4o-mini
# ROUTER_LLM_MODEL=gpt-4o-mini
# TRIAGE_LLM_MODEL=gpt-4o-mini
# SCOPE_LLM_MODEL=gpt-4o-mini

# Optional: live PartSelect fetch when DB has no part (default off)
# ENABLE_LIVE_PARTS_FETCH=0
