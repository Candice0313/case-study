{
  "name": "partselect-case-study",
  "version": "0.1.0",
  "private": true,
  "description": "PartSelect chat agent – frontend in apps/web (Next.js), API in apps/api (FastAPI)",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "api": "cd apps/api && uvicorn main:app --reload --port 8000"
  }
}
